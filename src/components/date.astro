---
interface Props {
  date: Date;
  classNames?: string;
}

const { date, classNames } = Astro.props;
---

<time datetime={date.toISOString()} class={classNames}>
  {
    date.toLocaleDateString("es-co", {
      year: "numeric",
      month: "long",
      day: "numeric",
    })
  }
</time>
